# פיצויי אצ"א – קובץ הקשר לפרויקט

קרא את `requirements.md` לפני כל שינוי בקוד. הוא המסמך הקובע.

## מה זה

PWA אישית שבודקת פיצויי אנשי צוות אוויר באל על. המשתמש מעלה שני קבצי PDF:

1. **תכנון** – "Individual duty plan" של NetLine/Crew, מתפרסם לפני החודש.
2. **ביצוע** – "Rainmaker – CrewPayDetails", בסוף החודש.

האפליקציה מחלצת נתונים, מריצה את החוקים מ‑`rules.json`, ומציגה: התראות תכנון, שינויים בין תכנון לביצוע, קרדיט ו‑RIG צפויים, ופערים מול מה שהחברה זיכתה (COM ו‑S/C).

## החלטות ארכיטקטורה

- **בלי שרת.** כל העיבוד בדפדפן. קובצי ה‑PDF לא יוצאים מהמכשיר.
- **אירוח:** GitHub Pages. אותה כתובת ל‑iPad (Safari) ול‑Windows (Edge/Chrome).
- **PWA:** manifest + service worker, עובד גם בלי רשת.
- **קריאת PDF:** PDF.js, עם מיקומי מילים (`getTextContent`) ולא טקסט גולמי. בלוח התכנון יש שלוש עמודות שהטקסט הגולמי שלהן מתערבב.
- **אחסון:** IndexedDB על המכשיר, עם כפתורי גיבוי ושחזור לקובץ.
- **שפה:** עברית, RTL.
- **מסמכים:** אין תהליך build. פיתוח מקומי: `python3 -m http.server`.

## כללי ברזל

- **לעולם לא לשמור קובצי PDF של המשתמש בריפוזיטורי.** הם מכילים שמות אנשי צוות ומספרי טלפון. הריפוזיטורי ציבורי. קובצי הדוגמה והתשובות יושבים מחוץ לריפוזיטורי, ב‑`%OneDrive%/Code_Dev/Claude/Elal_compensations/samples/`. `samples/` נשאר ב‑`.gitignore` ליתר ביטחון.
- **`rules.json` הוא המקור הקובע לחוקים.** אין להטמיע ערכי פיצוי בקוד. הקוד מכיר `logic.id` בלבד.
- **חוק לא נתמך אינו מדולג בשקט.** אם `logic.supported` הוא false או שה‑`logic.id` לא מוכר לקוד, מסך החוקים מסמן אותו "לא נתמך" עם הסיבה, ושורת הספירה שם מציינת כמה מהמוצגים אינם נתמכים. מסך הבדיקה לא מתריע על כך.
- **קוד לא מוכר בקבצים מוצג למשתמש**, לא מנוחש, ועם כל מה שדרוש כדי לעדכן את `rules.json` לפיו: התאריכים, מה שהדוח רשם באותם ימים, ותשובת המשתמש אם נשאל (בקשת בעל המוצר, 23/09/2026). קידומת בחוק שאינו קובע ערך יומי אינה הופכת קוד למוכר: SBY_FLD מוצג כלא מוכר, אף שחוקי הכוננות מזהים כל קוד SBY, כי אף חוק אינו יודע כמה מגיע עליו (`classifyCode` ב‑`js/rules/catalog.js`). `ctx.explainCode` ב‑`js/rules/evaluate.js`.
- **הערה ושאלה אינן מציינות את שם העמודה בדוח**, רק את מספר השעות, ומבחינות בין השניים: COM ו‑S/C הם "פיצוי", ו‑Credit ו‑RIG הם "קרדיט" (בעל המוצר, 23/09/2026). `amountWord` ב‑`js/rules/duty.js`. שם העמודה מופיע רק בטבלת ההשוואה, שם הוא המידע עצמו.
- **קרדיט מחושב לפי STA − STD** (מתוכנן). ATD/ATA משמשים רק לחוקים שתלויים בביצוע, כמו נחיתה מאוחרת.
- **בכל מקרה מעורפל האפליקציה שואלת את המשתמש**, ושומרת את התשובה בהיסטוריה של החודש. **שואלים רק כשהדוח לא זיכה** (החלטת בעל המוצר, 23/09/2026): כשהדוח כבר זיכה את הסכום, מעבר למה שחוקים אחרים מסבירים, האפליקציה מניחה את התשובה המזכה ורושמת את הנימוק. השאלה מנוסחת על העובדה שמזכה, כך ש"כן" = מגיע פיצוי ויש פער מול הדוח. `ctx.paidOn` ו‑`ctx.paidOnDate` ב‑`js/rules/evaluate.js` הן הבדיקה. יוצאת דופן: שאלת זיהוי של קוד לא מוכר.
- **כששאלה אחת יכולה לייתר אחרות, שואלים אחת בכל פעם**, לפי הסדר שממקסם את הסיכוי לעצור: בהרכב הצוות של נחיתות לילה הטיסה הארוכה ביותר קודם, כי תשובה שאינה נספרת מורידה את המספר מתחת למינימום ומייתרת את השאר (בקשת בעל המוצר, 23/09/2026).
- **שני חוקים שאינם יכולים לחול יחד חולקים שאלה אחת**, והתשובה בוחרת מי מהם חל: כך בשתי הדחיות של מיאמי (2026 ס' 24.4, לפני תחילת זמן התפקיד או אחריה), ששתיהן `logic.id` אחד עם `phase` שונה ומזהה שאלה משותף (בקשת בעל המוצר, 23/09/2026). החוק שרץ ראשון שואל, והשני קורא את התשובה ואינו שואל שוב.
- **הסיבה שסבב מתוכנן לא בוצע נשאלת במקום אחד בלבד**: בשאלה `cancelled:` שבחוק `cancelled_no_compensation`. חוק אחר שתלוי בסיבה קורא ממנה דרך `cancelStatus` ב‑`js/rules/duty.js`, ואינו שואל שאלה משלו (החלטת בעל המוצר, 23/09/2026). הקריאה היא לפי הסימונים שהחוקים הקודמים שמו על הסבב, ולכן היא מכסה גם את המקרה שבו לא נשאלה שאלה כי הדוח כבר זיכה. מ‑24/09/2026 השאלה הזאת נשאלת גם על סבב שבמקומו בוצע סבב אחר **באותם ימים**: טיסה אחרת באותם תאריכים אינה מוכיחה החלפה. ארבע התשובות: החלפה ביוזמת החברה (הגבוה מבין השתיים, כולל זכיה במכרז או סטיה לשדה משנה), החלפה מרצוני (רק מה שבוצע) – שתיהן עם קישור לטיסה שבוצעה במקום, הטיסה המקורית בוטלה ללא קרדיט (ימי הסבב נספרים כימים ללא פעילות, ועל מה שבוצע בהם מגיעה קריאה מיוחדת), והורדה מהטיסה המקורית (השעות שהפסיד, בנוסף לקרדיט של מה שבוצע). לא שואלים רק כשהדוח כבר זיכה קריאה מיוחדת על הסבב שבוצע, או את השעות שהפסיד על סבב שלא בוצע; זיכוי של ההפרש בלבד אינו מבטל את השאלה. **סטיה לשדה משנה** (הסבב שבוצע כולל, לצד היעד המתוכנן היחיד, עוד יעד אחד בלבד) גם היא לא נשאלת: מונחת אוטומטית כהחלפה ביוזמת החברה, כי הקרדיט כבר כולל את כל הרגליים שבוצעו (`assumeDiversion` ב‑`js/rules/logic.js`, בעל המוצר 26/09/2026; עוד לא אומת מול דוח אמיתי).
- **"הגבוה מבין שתי הטיסות" (`higher_of_planned_performed`) הוא סך הכול, לא תוספת על ההשלמה למינימום לסליפ קצר** (`min_slip_credit`) שהסבב שבוצע מקבל בזכות עצמו: הציפייה היא רק להפרש שמעבר להשלמה שכבר צפויה, וכשההשלמה למינימום כבר גבוהה מההפרש – אין תוספת נוספת כלל (בעל המוצר, 24/09/2026). `higher_of_planned_performed` קוראת ל‑`minSlipTopUp` (המשמשת גם את `voluntary_swap`) כדי לדעת כמה כבר צפוי, ומצפה רק לנוסף.
- **כל תשובה שקושרת סבב מתוכנן לטיסה אחרת (או להפך) נבדקת מול מה שכבר נקבע על הצד השני**, בין אם זו תשובה של המשתמש ובין אם זו הנחה מהדוח (`wouldAssumeCancelled`, כמו ב‑`assumeCancelled`): "החלפה ביוזמת החברה" ו"החלפה מרצוני" קובעות שמגיע (או לא מגיע) משהו על הסבב המתוכנן, ו"החלפה מרצוני" מהצד השני קובעת שאין תוספת על הטיסה שהחליפה אותו כי היא כבר "קריאה מיוחדת" בזכות עצמה. שתי קביעות סותרות על אותו זוג טיסות לא יכולות לחול יחד. כשזה קורה, האפליקציה שואלת שאלה אחת שממוקדת בסבב המתוכנן עצמו – לא "איזו משתי ההחלטות נכונה" בלשון מופשטת, אלא מה קרה בו בפועל, עם הלשון הקונקרטית של כל תביעה ושם הטיסה השנייה (`swap_conflict:`, בעל המוצר, 24/09/2026) – ומקפיאה את שני הצדדים עד לתשובה, כדי לא לשלם על שניהם. `checkLinkConflicts` ו‑`resolveLinkConflict` ב‑`js/rules/logic.js`, רצות בתחילת `cancelled_no_compensation` לפני שאר החוקים; `claimLabel` בונה את הניסוח, ו‑`whatHappenedOptions` משותפת לשאלה הזאת ולשאלה הרגילה על הסבב (`cancelled:`) כדי שאותו ערך תמיד יתואר באותה לשון. לא נבדק המקרה שבו שני סבבים מתוכננים שונים מקשרים לאותה טיסה שבוצעה.

## הסכמי העבודה

התיקייה `הסכמי עבודה/` מכילה את ההסכמים שמהם נגזרים החוקים: ישן (הבסיס), 2018, 2024, 2026. היא יושבת מחוץ לריפוזיטורי, ב‑`%OneDrive%/Code_Dev/Claude/Elal_compensations/הסכמי עבודה/`. **אסור להעלות אותה**, והיא נשארת ב‑`.gitignore`. כל הסכם מבטל או מעדכן את מה שלפניו, וסעיף שלא עודכן נשאר בתוקף. הסכם 2026 סרוק ואין בו שכבת טקסט; קוראים אותו כתמונה.

## מבנה `rules.json`

- `rules_version` – נבדק בכל ריצה. בדיקה של חודש קודם רצה לפי החוקים שהיו בתוקף אז (`valid_from` / `valid_to`).
- כל חוק: `id`, `category` (plan / exec / train), `title`, `when`, `amount`, `source`, `status` (ok / cancelled / changed), `logic.id`, `logic.supported`, `logic.params`.
- `codes` – מיפוי קודים, כולל הקודים המקוצרים ל‑5 תווים שמופיעים בדוח הביצוע.
- 49 חוקים מסומנים `supported: true`. ההיקף: תכנון, ביצוע והדרכה. עשרים ושניים מהם תלויים בזמני התפקיד (FDP, מנוחה, נחיתות לילה, תאריכים מיוחדים, טיסה לבנה, טיסת ULH, ימים ללא פעילות, שבתות ברצף, טיסות סבב לילה עוקבות, הארכת שהייה, שלושת חוקי מיאמי, קיצור מנוחה בלאס וגאס, וסימולטור בישראל בלילה, במוצ"ש, בשישי ובערב חג ובהארכה, לפי שורות SIM בדוח הביצוע; ימי החג ב‑`holidays` שבראש `rules.json`), ב‑`js/rules/duty.js`; ראו `requirements.md` סעיף 8.1. השאר הם היסטוריה או דורשים מידע שאינו בקבצים. **צי רחב גוף אינו סייג היקף** (החלטת בעל המוצר, 23/09/2026): טיסה לבנה, טיסת ULH, מיאמי וקיצור המנוחה בלאס וגאס נתמכים. הדבר היחיד שמחוץ להיקף הוא פיצויי המדריכים, הבוחנים והממיינים (`out_of_scope`; `requirements.md` סעיף 13). טיסת ULH (2026 ס' 21.2) נתמכת: בלוק מתוכנן מעל 17 ועד 19 שעות → 5 ש' קרדיט, בהנחת צוות כפול לפי ס' 21.1. שלושת חוקי מיאמי (2026 ס' 24) נתמכים מ‑23/09/2026: קיצור מנוחה כשהצוות שהה שם לילה אחד בלבד (23:00–05:00 בשעון מיאמי), ושתי הדחיות ביציאה מהארץ, שחלות רק בנוסף לו. קיצור המנוחה בלאס וגאס (2018 ס' 58.2) נתמך מ‑23/09/2026: ההסדר קבוע, ולכן כל שהייה ב‑LAS מזכה ב‑5 ש' בלי תנאי על אורך השהייה, ושואלים רק כשהדוח לא זיכה (מנוחה מלאה, או שהחברה אינה טסה לשם פעם בשבוע – שתיהן אינן בקבצים).

## בדיקות

`requirements.md` סעיף 14 מכיל שישה תרחישים מאומתים מול הנתונים האמיתיים: **יולי 2026**, **אוגוסט 2026**, **ינואר 2026**, **פברואר 2026**, **מאי 2026** ו**יוני 2026** (ינואר, פברואר ומאי בלי קובץ תשובות; ביוני תשובה אחת: FRA → BER החלפה מרצון). כל שינוי במנוע החוקים צריך להמשיך לעבור אותם. השורות שמסומנות ✓ אומתו מול דוחות אמיתיים, כולל:

- נחיתה מאוחרת בארץ: 62 דק' → 0.5; 141 דק' → 1.0; 93 דק' → 0.5; 37 ו‑33 דק' → 0. כל איחור מ‑30 דק' ומעלה נרשם כהערה (`note_from_minutes`, בעל המוצר, 24/09/2026), גם כשמגיע עליו פיצוי, בלי לציין כמה דקות נותרו מעבר לסף – רק כמה מדרגות הן וכמה פיצוי יוצא מהן. משך האיחור עצמו בדקות עד שעה, ובשעות (H:MM) מעבר לשעה: 93 דק' מוצג "1:33 שעות" (בעל המוצר, 24/09/2026).
- חופשה: Credit 03:45 ליום. מחלה מתוכננת: 02:30. אימון קרקע (HOME_, RGT, INV_C) וסימולטור: 03:45.
- יולי, אוגוסט ויוני 2026, עם התשובות ב‑`samples/answers-2026-0?.json` שבתיקיית OneDrive, תואמים את הדוח בכל השורות והסיכומים.
- פברואר 2026: חזרה לבסיס אחרי המראה (LY2367 TLV→TLV ב‑15) היא חלק מהסבב; סבב שחוזר בחודש הבא (28/02) מקבל S/C עד סוף החודש ובלי השלמה ל‑5 שעות.
- מאי 2026: "Mon01" בסוף התכנון הוא 1 ביוני ולא נקרא; LY387 ב‑31/05 חוצה את סוף החודש → Credit 04:22 (מ‑ATD עד חצות).
- יוני 2026: הדוח חוזר ב‑01/06 על LY387 שיצאה ב‑31/05 (TAB 24:00) → Credit הסבב 03:53 = 08:15 − 04:22; SBY_S → 04:15; CPT_B ו‑CRM_C → 03:45; ה‑Rig 05:10 של LTN → OTP (11/06) נרשם על OTP של 10/06, אחרי תשובה "החלפה ביוזמת החברה (כולל זכיה במכרז)" (האפליקציה לא מניחה החלפה, גם כשה‑Rig שווה להפרש ונרשם בדוח); גם BUD ‏14–15/06 שהוחלף ב‑WAW נשאל, בלי הפרש לתשלום.
- יוני 2026: HOME_RGT שזז מ‑07 ל‑09 → הערה, בלי פיצוי; הטיסה שהתבצעה באותם ימים (BER) אינה קשורה ואינה מוזכרת בהערה, כי היא כבר מוסברת בזכות עצמה (החלפה מרצונית של FRA). קריאה מחופשה, סימולטור שבוטל הפעלה כאיש צוות פעיל (DH בתכנון, LEG בביצוע) וטיסה לבנה (787/777 בלבד; הרכב הצוות נשאל) נבדקו רק על נתונים סינתטיים (סעיף 14.10).
- היום הראשון ללימודים: התאריך אינו בקבצים ומשתנה משנה לשנה, ולכן בכל ספטמבר הוא נשאל ביומן (בעל המוצר, 24/09/2026), עם ברירת מחדל 1/9 – או 2/9 אם 1/9 חל בשבת. זו השאלה היחידה שהתשובה בה היא תאריך ולא אפשרות (`dateInput` בשאלה, `ask_date` באירוע שב‑`rules.json`). אין בידינו קובץ של ספטמבר, והבדיקה היא על תכנון יוני 2026 שהוזז ל‑ספטמבר (סעיף 14.25).
- שתי שבתות ברצף: חלון השבת קבוע, משישי 20:00 עד שבת 16:00 (בעל המוצר, 23/09/2026), ורק טיסה שחופפת לו נספרת – פעילות קרקע לא. יוני 2025 (HOME_RGT ב‑21/06 וב‑28/06) הוא המקרה שחשף זאת. חופשה ללא תשלום (VAC_NOC) אינה מזכה בכלום, ונרשמת כהערה בלבד.
- ימים ללא פעילות: יום עם נחיתה בבסיס עד 01:00 פנוי; X אינו בהכרח יום פנוי (מופיע גם באמצע סבב). ימי סבב שבוטל ללא קרדיט נספרים כפנויים (יולי 2026: 14 → 15, מול מינימום 9). אוגוסט 2026: 89:03 → מינימום 11, בתכנון 10 (חסר יום אחד → הערה בלי פיצוי). אין בחודשים שבדקנו שתי שבתות עוקבות עם פעילות.
- שני סבבים באותו FDP מקבלים את המינימום יחד: BUS ‏05:04 + LCA ‏02:15 ב‑25/11/2025 וב‑30/12/2025 → Rig 02:41 = 2 × 5:00 − 07:19. אוקטובר ונובמבר 2025 תואמים את הדוח, ודצמבר אחרי התשובה על GVA ‏15–16/12 שהוחלף ב‑BER. בנובמבר COM 07:30 על DME ב‑11/11 (שורה ידנית): 02:30 על טיסה מעל 12 שעות, ו‑05:00 שאף חוק אינו מסביר → ירוק עם הערה "יכול להיות שהטיסה הופעלה בצוות חוקי במקום חוזי" (לא מנחשים את הפיצוי הזה, רק מסבירים אותו כשהוא בדוח). השעות בשורת SIM אינן מדויקות: משך = STA − STD, ולשעות חריגות האימון מתחיל רבע שעה אחרי STD ומסתיים רבע שעה לפני STA. לכן SIM ב‑09/11 עם STD 06:52 אינו אימון לילה.
- נובמבר 2025: SIM_BER באמצע סבב BER → Credit 03:45 ביום, TAB של השהייה, וסימון SIM ביום תחילת הסבב (17/11).
- ינואר 2025: הדוח רושם רגל ביום ה‑STD בשעון הבסיס (LY398 ‏MAD ‏STD 23:05 מקומי = 00:05 → 24/01, ‏04:55). ‏DH בחברה אחרת: בתכנון `DH/LX 1487`, בדוח DHX בלי מספר טיסה → PRG→ZRH ‏01:20 ב‑28/01, יחד עם SIM_PRG. גם במרץ 2025 (DH/LX 645 ‏CDG→ZRH). פברואר 2025: ‏RGT (אותו קוד בתכנון ובדוח) הוא פעילות קרקע כמו HOME_RGT → 03:45 ליום.
- ינואר 2026: DME 12:45 → COM 02:30; SCK_P → 02:30 וסימון ב‑SICK; HOME_RGT שזז מ‑07 ל‑05 מזוכה רק ב‑05.

הרצת המנוע על חודש: `node tools/run-month.mjs --plan <samples>/… --exec <samples>/… [--answers <samples>/answers-….json]`, כש‑`<samples>` הוא נתיב התיקייה ב‑OneDrive. קובצי התשובות יושבים שם כי הם מכילים מזהי סבבים אישיים.

## מבנה הקוד

- `index.html`, `css/app.css`, `js/app.js` – הממשק: העלאה, תוצאות, שאלות, היסטוריה, מסך חוקים. ייצוא PDF של הדוח הוא `window.print()` עם סגנון הדפסה; טבלת החוקים מיוצאת ל‑Excel (`js/xlsx.js`).
- `js/store.js` – IndexedDB. לכל חודש נשמרים הנתונים שחולצו (לא קובצי ה‑PDF), התשובות וגרסת החוקים. גיבוי ושחזור לקובץ JSON.
- `js/pdf/` – חילוץ, `js/model.js` – ציר ימים וסבבים, `js/airports.js` – אזור הזמן של כל שדה (שדה חדש נוסף שם), `js/rules/` – קטלוג, לוגיקות (`logic.js`, וחוקי זמני התפקיד ב‑`duty.js`), `evaluate.js`.
- `sw.js` – רשת קודם, מטמון כגיבוי, ורק לקבצים שברשימת `SHELL`. **קובץ JS חדש נכנס ל‑`SHELL`, ובכל שינוי ברשימה מעלים את `CACHE`.**
- בדיקה בדפדפן: השרת המקומי מגיש רק את הריפוזיטורי. כדי לטעון דוגמה לשדה ההעלאה עם `fetch` + `DataTransfer`, מעתיקים אותה זמנית ל‑`samples/` בריפוזיטורי (שב‑`.gitignore`) ומוחקים אחרי הבדיקה.

## מה עוד פתוח

`requirements.md` סעיף 15. מדרגות איזון החופשה הושלמו (2026-09-21.1); החישוב הוא תוספת ליום × ימים, ו‑`taper_monthly_totals` לא בשימוש.

- **פרמטר חדש בחוק דורש קוד.** `KNOWN_PARAMS` ב‑`js/rules/logic.js` מחזיק את הפרמטרים שכל `logic.id` מכיר. פרמטר שלא ברשימה הופך את החוק ל"לא נתמך". כשמוסיפים פרמטר ל‑`rules.json`, מוסיפים גם את הקוד שמשתמש בו ואת השם ל‑`KNOWN_PARAMS`.
